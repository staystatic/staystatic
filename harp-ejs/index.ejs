div
  b News 'n' Updates
  ul(class="news")
    each post, slug in public.posts._data
      li
        - page = post.page
        a(href="#{ site.path }/posts/#{ slug }") #{ page.title }

div
  b Links 'n' Bookmarks
  ul(class="links")
    each link in public.data._data.links
      li
        a(href="#{ link.url }") #{ link.title }
